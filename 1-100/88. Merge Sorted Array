class Solution {
    public void merge(int[] nums1, int m, int[] nums2, int n) {
        for (int i = m,count = 0; i < m + n; i++) {
            nums1[i] = nums2[count++];
        }
        Arrays.sort(nums1);
    }
}