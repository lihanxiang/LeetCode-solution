//只检测左半边符号，只推入右半边符号
class Solution {
    public boolean isValid(String s) {
        char[] c = s.toCharArray();
        Stack<Character> stack = new Stack<>();
        for (int i = 0; i < c.length; i++) {
            if (c[i] == '(') {
                stack.push(')');
            } else if (c[i] == '[') {
                stack.push(']');
            } else if (c[i] == '{') {
                stack.push('}');
            } else if (stack.size() == 0 || stack.pop() != c[i]){
                return false;
            }
        }
        return stack.size() == 0;
    }
}