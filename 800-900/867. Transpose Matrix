/* result 数组的行和列对应 A 数组的列和行，比如用例中的第二个
 * [[1,2,3],[4,5,6]]是 2 * 3，所以我们在创建 result 数组是就要
 * 初始化为 3 * 2 的
class Solution {
    public int[][] transpose(int[][] A) {
        int[][] result = new int[A[0].length][A.length];
        for (int i = 0; i < A.length; i++) {
            for (int j = 0; j < A[i].length; j++) {
                result[j][i] = A[i][j];
            }
        }
        return result;
    }
}