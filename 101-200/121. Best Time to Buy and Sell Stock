//两个指针移动，将每个数和它之后的每个数对比，如果 max 还是 -1，就设为 0
class Solution {
    public int maxProfit(int[] prices) {
        int max = -1, temp;
        for (int i = 0; i < prices.length; i++) {
            for (int j = i + 1; j < prices.length; j++) {
                temp = prices[j] - prices[i];
                max = temp > max ? temp : max;
            }
        }
        max = max < 0 ? 0 : max;
        return max;
    }
}