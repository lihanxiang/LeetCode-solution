class Solution {
    public boolean repeatedSubstringPattern(String s) {
        /* 其实这个答案的针对性很明显，就是把字符串看作是有重复子串的，
         * 比如 s = abab, str = abababab, s 看作是两个 ab 组成的，组成 str
         * 之后，首位各去掉一位，这时候，两个 s 各剩下一个 ab，刚好能组成一个 s
         * 如果不能组成，就表示 s 没有重复子串
         */
        String str = s + s;
        return str.substring(1, str.length() - 1).contains(s);
    }
}